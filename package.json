{
  "name": "timewise",
  "private": true,
  "packageManager": "pnpm@10.34.5",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "dev:mobile": "pnpm --filter mobile start",
    "build": "pnpm --filter web build",
    "lint": "pnpm --filter web lint",
    "test": "pnpm --filter @timewise/core test && pnpm --filter web test"
  },
  "engines": {
    "node": ">=20"
  }
}
